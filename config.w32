// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("anprox", "for anprox support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("anprox", "enable anprox support", "no");

if (PHP_ANPROX != "no") {
	EXTENSION("anprox", "anprox.c");
}

